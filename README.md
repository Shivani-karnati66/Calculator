# Calculator
This is a simple GUI-based Calculator built using Java AWT. It allows users to perform basic arithmetic operations like addition, subtraction, multiplication, and division. The project follows object-oriented programming (OOP) by using an abstract class for operations. The calculator has a user-friendly interface with buttons for digits and operators. It also includes error handling to prevent division by zero.
